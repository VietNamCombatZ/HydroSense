# Copy to .env and set values
ARCGIS_API_KEY=
BACKEND_PORT=8000
FRONTEND_PORT=5173
# For demo only: expose API key to frontend via /api/config so basemaps load
EXPOSE_API_KEY=true
# Optional: persist floods to file (backend/floods.json)
PERSIST_FLOODS=false
